// ============================================================
// ejemplo.txt — Programa VÁLIDO para el mini lenguaje C++
// Este archivo demuestra todas las construcciones soportadas
// ============================================================

// --- Declaraciones de variables ---
int x = 10;
int y = 20;
double pi = 3.14;
double temperatura = 36.6;
char inicial = 'J';

// --- Variable sin valor inicial ---
int resultado;

// --- Asignaciones ---
x = x + y;
resultado = x * 2;

// --- Expresiones aritméticas ---
int suma = x + y;
int diferencia = x - y;
int producto = x * y;
int cociente = x / 2;
int resto = x % 3;

// --- Expresiones con paréntesis ---
int complejo = (x + y) * 2;
int otro = x + (y * 3) - 1;

// --- cout: salida por pantalla ---
cout << x;
cout << suma;
cout << inicial;
cout << (x + y) * 2;

// --- if simple ---
if (x > 10) {
    cout << x;
}

// --- if con else ---
if (x > 5) {
    cout << x;
} else {
    cout << y;
}

// --- if con expresión compleja ---
if (x > 0 && y < 100) {
    resultado = x + y;
    cout << resultado;
}

// --- while básico ---
while (x < 100) {
    x = x + 1;
}

// --- while con condición compuesta ---
while (x > 0 && x < 200) {
    x = x - 1;
    cout << x;
}

// --- Expresiones booleanas ---
int esPositivo = x > 0;
int enRango = x >= 0 && x <= 100;
int esCero = x == 0;
int noEsCero = x != 0;
int negacion = !esPositivo;

// --- Bloques anidados ---
if (x > 0) {
    int local = x * 2;
    if (local > 50) {
        cout << local;
    }
}

// --- Expresión negativa ---
int negativo = -5;
int calculado = -(x + y);
